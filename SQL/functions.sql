/* Faça aqui as function, aqui vai um exemplo para referencia:
-- Função para calcular o custo por funcionário (salário + R$50 por falta)
CREATE FUNCTION CustoFuncionario(salario DECIMAL, faltas INT)
RETURNS DECIMAL
AS
    RETURN salario + (faltas \* 50);

*/